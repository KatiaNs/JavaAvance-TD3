package model;

// Creating an interface for shapes
public interface Shapes {

	public double area();
	public boolean contains(Point p);
	public String toString();
	public void drawShape();
}
